# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
hdbscan>=0.8.0

# DTW libraries (install at least one)
dtaidistance>=2.3.0  # Recommended: Fast C-based DTW
# fastdtw>=0.3.4     # Alternative: Pure Python (slower) - uncomment if dtaidistance fails

# Visualization
dash>=2.0.0
plotly>=5.0.0

# Dimensionality reduction
umap-learn>=0.5.0  # Recommended for better embeddings
# If umap-learn fails to install, t-SNE (included in scikit-learn) will be used

# Utilities
tqdm>=4.62.0
esprima>=4.0.1
tree_sitter>=0.25.0
tree_sitter_javascript>=0.25.0
gensim>=4.3.0
